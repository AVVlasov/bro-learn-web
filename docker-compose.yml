version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: bro-learn-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: brolearn
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
