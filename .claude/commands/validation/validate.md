# Project Validation Command

Комплексная проверка проекта bro-learn-web.

Выполни следующие команды последовательно и предоставь отчет о результатах.

---

## 1. Linting

### Frontend ESLint

```bash
npm run eslint
```

**Ожидаемый результат:** Нет ошибок или все ошибки относятся к стилю, а не к функциональности.

---

## 2. TypeScript Compilation

```bash
npm run compile
```

**Ожидаемый результат:** Успешная компиляция без ошибок типизации.

---

## 3. Frontend Build

### Development Build

```bash
npm run build
```

**Ожидаемый результат:** 
- Build завершается успешно
- Создается директория `dist/`
- Размер бандла разумный (< 5MB для development)

### Production Build (опционально)

```bash
npm run build:prod
```

**Ожидаемый результат:** 
- Build завершается успешно
- Код минифицирован и оптимизирован
- Размер бандла меньше development версии

---

## 4. MongoDB Connection Check

Используй MCP MongoDB для проверки подключения и состояния базы данных.

### 4.1 Проверка списка баз данных

```bash
mcp_MCP_DOCKER_list-databases
```

**Ожидаемый результат:** База данных `bro-learn` присутствует в списке.

### 4.2 Проверка коллекций

```bash
mcp_MCP_DOCKER_list-collections database="bro-learn"
```

**Ожидаемый результат:** Список коллекций проекта (lessons, progress, categories, и т.д.).

### 4.3 Статистика базы данных

```bash
mcp_MCP_DOCKER_db-stats database="bro-learn"
```

**Ожидаемый результат:** Статистика показывает размер БД, количество коллекций, индексов.

### 4.4 Проверка индексов

Для каждой основной коллекции:

```bash
mcp_MCP_DOCKER_collection-indexes database="bro-learn" collection="lessons"
mcp_MCP_DOCKER_collection-indexes database="bro-learn" collection="progress"
mcp_MCP_DOCKER_collection-indexes database="bro-learn" collection="categories"
```

**Ожидаемый результат:** Индексы созданы на часто используемых полях.

---

## 5. API Endpoints Validation

### 5.1 Запуск сервера (если не запущен)

Проверь терминалы:

```bash
# Список терминалов
ls -la c:\Users\Andrey\.cursor\projects\f-develop-bro-learn-web\terminals\
```

Если сервер не запущен, запусти:

```bash
npm start &
```

Подожди 3-5 секунд для инициализации сервера.

### 5.2 Проверка базового пути API

Используй **браузер** (не curl!) для проверки:

1. Открой браузер
2. Перейди на `http://localhost:8099/assessment-tools`
3. Открой DevTools (F12) → Network tab
4. Выполни действия, которые вызывают API запросы

**Ожидаемый результат:** 
- Все API запросы идут на `/api/*`
- Статус коды: 200 для успешных запросов
- Ответы в формате JSON
- Время ответа < 1 секунда

### 5.3 Проверка основных endpoints

Через браузер DevTools проверь следующие endpoints:

```
GET  /api/lessons          - список уроков (200)
GET  /api/lessons/:id      - конкретный урок (200 или 404)
POST /api/lessons          - создание урока (201)
PUT  /api/lessons/:id      - обновление урока (200)
DELETE /api/lessons/:id    - удаление урока (204)

GET  /api/categories       - список категорий (200)
GET  /api/progress         - прогресс пользователя (200)
POST /api/progress         - обновление прогресса (200 или 201)
```

**Ожидаемый результат для каждого endpoint:**
- ✅ Статус код соответствует ожидаемому
- ✅ Формат ответа валидный JSON
- ✅ Данные читаются/пишутся в MongoDB (не моки!)
- ✅ Нет ошибок в консоли браузера
- ✅ Нет ошибок в логах сервера

---

## 6. Data Integrity Check

### 6.1 Проверка данных в коллекциях

```bash
# Количество записей в каждой коллекции
mcp_MCP_DOCKER_count database="bro-learn" collection="lessons"
mcp_MCP_DOCKER_count database="bro-learn" collection="progress"
mcp_MCP_DOCKER_count database="bro-learn" collection="categories"

# Примеры данных
mcp_MCP_DOCKER_find database="bro-learn" collection="lessons" limit=3
mcp_MCP_DOCKER_find database="bro-learn" collection="progress" limit=3
```

**Ожидаемый результат:**
- Данные существуют и имеют правильную структуру
- Обязательные поля заполнены
- Типы данных соответствуют ожидаемым
- Timestamps (createdAt, updatedAt) присутствуют

### 6.2 Проверка схемы коллекций

```bash
mcp_MCP_DOCKER_collection-schema database="bro-learn" collection="lessons" sampleSize=50
```

**Ожидаемый результат:** Схема соответствует документации проекта.

---

## 7. UI/UX Validation

### 7.1 Контрастность и читаемость

Открой приложение в браузере и проверь:

- [ ] Текст на темном фоне использует белый или светлые цвета (#FFFFFF, #E0E0E0)
- [ ] Текст на светлом фоне использует темные цвета (#0A0A0A, #1A1A1A)
- [ ] Иконки четко видны и контрастны
- [ ] Кнопки действий (редактирование, удаление) имеют яркие цвета
- [ ] Нет серых иконок на темном фоне
- [ ] Критичные действия (удаление) выделены красным (#FF4444)

### 7.2 Локализация

Проверь через браузер:

- [ ] Все тексты отображаются (нет ключей типа `common.button.submit`)
- [ ] Переключение языка работает (если реализовано)
- [ ] Все кнопки и лейблы переведены

### 7.3 Ошибки в консоли

Открой DevTools → Console:

- [ ] Нет ошибок React (красные сообщения)
- [ ] Нет предупреждений о ключах в списках
- [ ] Нет 404 ошибок загрузки ресурсов
- [ ] Нет CORS ошибок

---

## 8. Performance Check

### 8.1 Bundle Size

После build проверь размер файлов:

```bash
ls -lh dist/
```

**Ожидаемый результат:**
- main.bundle.js: < 2MB (development) / < 500KB (production)
- vendor.bundle.js: < 1MB
- CSS файлы: < 200KB

### 8.2 API Response Time

В браузере DevTools → Network:

- [ ] API запросы отвечают < 500ms
- [ ] Нет дублирующихся запросов
- [ ] Кэширование работает (304 статусы для статики)

### 8.3 Hot Reload

Внеси изменение в любой файл и проверь:

- [ ] Браузер автоматически перезагружается
- [ ] Изменения видны без ручного обновления
- [ ] Не требуется перезапуск сервера

---

## 9. Code Quality Check

### 9.1 No Hardcoded Values

Проверь, что нет хардкода:

```bash
# Поиск хардкода /api
grep -r "'/api" src/ --exclude-dir=node_modules --exclude-dir=dist

# Должны быть только импорты URLs.apiBase
grep -r "URLs.apiBase" src/
```

**Ожидаемый результат:** 
- ❌ Нет хардкода `'/api'` в fetch или axios
- ✅ Используется `URLs.apiBase` из `src/__data__/urls.ts`

### 9.2 TypeScript Strict Mode

Проверь `tsconfig.json`:

```json
{
  "compilerOptions": {
    "strict": true,
    ...
  }
}
```

### 9.3 No Mocks in Production

Проверь API файлы:

```bash
grep -r "let.*=.*\[\]" stubs/api/ --exclude=node_modules
grep -r "const.*=.*\[\]" stubs/api/ --exclude=node_modules
```

**Ожидаемый результат:** Нет глобальных массивов или объектов для хранения данных.

---

## 10. Summary Report

После выполнения всех проверок создай итоговый отчет:

### ✅ Passed Checks

- [ ] Linting: без критичных ошибок
- [ ] TypeScript: компиляция успешна
- [ ] Build: development и production сборки работают
- [ ] MongoDB: подключение установлено, коллекции существуют
- [ ] API: все endpoints отвечают корректными статус-кодами
- [ ] Data: данные в БД валидные, схема корректная
- [ ] UI/UX: контрастность хорошая, локализация полная
- [ ] Performance: bundle size и response time в норме
- [ ] Code Quality: нет хардкода, используется strict mode
- [ ] Hot Reload: работает без перезапуска сервера

### ❌ Failed Checks

Список проблем с описанием:

1. **[Категория]**: Описание проблемы
   - Ожидалось: ...
   - Получено: ...
   - Решение: ...

### 📊 Statistics

- **Total API Endpoints**: X
- **Successful Requests**: X/X (XX%)
- **MongoDB Collections**: X
- **Total Documents**: X
- **Bundle Size (dev)**: X MB
- **Bundle Size (prod)**: X KB
- **Average API Response Time**: X ms

### 🎯 Overall Health

**[PASS / FAIL]**

- **Pass**: Все критичные проверки пройдены, проект готов к работе
- **Fail**: Есть критичные проблемы, требующие исправления

### 📝 Recommendations

1. ...
2. ...
3. ...

---

## Notes

- **НЕ используй автотесты** для проверки UI - всегда используй браузер
- **Всегда проверяй через MCP MongoDB** состояние данных
- **Не останавливайся при ошибках** - составь план исправления и выполни его
- **Не создавай отдельных файлов** с отчетами - выведи результаты в консоль
